# Input two lists from the user
list1 = input("Enter the first list (space-separated values): ").split()
list2 = input("Enter the second list (space-separated values): ").split()

# Concatenate the lists
combined_list = list1+list2

print("Combined List:", combined_list)
